raw_decks = fetch_deck_names()
# decks = ["Select deck"] + raw_decks
# selected_deck = ctk.StringVar(value="Select deck")
# deck_dropdown = ctk.CTkOptionMenu(app, values=decks, variable=selected_deck)
# deck_dropdown.pack(pady=10)